# v1.6 实现差距清单（对照 `project_20260322_172238` / Next+Supabase 工程）

**需求主本**：同目录 `采购管理系统需求规格说明书_v1.6.md`（§10.3 全量索引）。

**被评对象**：压缩包 `project_20260322_172238.tar.gz` 解压后的工程，路径 **`_review_extract/project_172238/projects`**（旧版 `170854` 对照见文末说明）。代码迁走后请改路径并重评状态列。

**状态图例**：`[ ]` 未满足 · `[~]` 部分满足 · `[x]` 基本满足（仍可能有细节缺口）

**已知风险**：`goods-receipts` 等 API 使用 `status` / `is_overdelivery` / `approved_by` 等列时，须与 **Supabase 实际表结构**一致；当前 Drizzle `schema.ts` 中 `goods_receipts` 定义可能未包含全部运行字段，部署前请核对迁移。

---

## §10.3 功能需求全量索引 — 勾选表

| # | 需求概要 | 主条款 | 状态 | 说明（相对 172238 快照） |
|---|----------|--------|------|-------------------------|
| 1 | 项目背景与 Agent 目标用户 | §1.1–§1.2 | [x] | Web 全栈 + API，方向符合「面向 Agent/人机」 |
| 2 | 交付范围（全量） | §1.3 | [ ] | FA→自动 PO、飞书真发送与绑定进线、全 API 一致鉴权等仍缺，整体未达全量 |
| 3 | 三角色职责 | §2.1 | [x] | 模型与页面分工可体现三角色 |
| 4 | 权限矩阵；飞书三入口、绑定与隔离 | §2.2、§2.5 | [~] | `role-filter` + PR 列表按角色过滤；**多数**路由仍手写头或未统一；飞书仅有表 + `feishu-notification` 骨架，**无**三入口业务闭环 |
| 5 | 信任模式与风险 | §2.3 | [~] | 仍依赖 `X-Actor`/`X-Role`，与「会话写入身份」不符 |
| 6 | 按角色查询范围 | §2.4 | [~] | PR GET 已 `filterPurchaseRequests`；PO 等列表**未**全面套用 `role-filter`（与 PR 不一致） |
| 7 | 采购申请 PR | §3.1 | [~] | `numberGenerators.pr`：**上海日界 + 99 上限**；申请人等仍来自请求头 |
| 8 | 寻源任务 SC | §3.2 | [~] | 表与 API 具备；**审批后未命中 FA 自动建 SC**（`approve/route.ts`）；行进度置 `sourced` |
| 9 | 报价单（单一中标） | §3.3 | [~] | `generateQuoteNumber`：RPC 成功时为 **Q-+6 位**；失败回退 **QT-** 日流水 |
| 10 | 框架协议 FA | §3.4 | [~] | 有 FA；字段与 §3.4 细节未完全对齐；匹配仍为**原文归一相等** |
| 11 | 采购订单 PO | §3.5 | [~] | `numberGenerators.po`；有 PO/行与 `pending_qty`；**仍缺** FA 路径自动合并/创建及决策 34 |
| 12 | 收货单 GR | §3.6 | [~] | `generateGRNumber`：上海 + 99；退货仍 **RT-** 前缀（与「统一 GR + 类型」需对条文） |
| 13 | PR 流程 FA/寻源/PO 合并 | §4.1、§5.6 | [~] | 批后：文本匹配 → `matched_protocol`；**未命中 → 自动 SC**；**仍无**合并/自动 PO |
| 14 | 收货与超收分支 | §4.2 | [~] | **>5%** 且非 `manager`：`pending_approval` + `approve-overdelivery`；经理直收仍可走正常入账 |
| 15 | 退货与补货 | §4.3 | [~] | `gr_type=out` 与数量回冲；PR 行「退货待补货」等与规格逐项核对 |
| 16 | 框架协议匹配规则 | §5.1 | [ ] | 未以 **material_id** 为主、无最低价择优；**有效期**在 `approve` 内仍 **`toISOString` 日历日（UTC）**，与取号用**上海日**不一致 |
| 17 | 口语化识别与用户确认 | §5.1.1 | [ ] | 命中仍 **静默** `match_confirm: 'confirmed'`，无 Top-N 与强制确认 |
| 18 | 主数据启停用 | §5.1.2 | [~] | 有 `is_active`；拦截与 §8.3 启停日志需核对 |
| 19 | 超收与 §5.2.1 留痕 | §5.2、§5.2.1 | [~] | 超收待批/通过/拒绝有 `audit_logs` 与 GR 扩展字段意图；是否满足 §5.2.1 **最小字段集**需对照主本逐项勾选 |
| 20 | PR/PO 行状态 + FA PO 失败重试 | §5.3 | [ ] | **无**决策 **34**（PO 失败 3×1min + 显式重试） |
| 21 | 未收货（净收货 Q_in/Q_out） | §5.4 | [~] | 常路径仍累加 `received_qty`；超收审批通过后更新逻辑需与 GR 汇总、多行 PO 联调验证 |
| 22 | 单据提交后不可改 | §5.5 | [~] | 未见统一「禁止改事实字段」中间件 |
| 23 | 编号、并发、99 上限、上海时区 | §6 | [~] | **`number-generator.ts`**：上海日 + 99 + RPC 可选；RPC 不可用时 **count 回退**并发下非严格防重；业务里 **FA 窗口日**仍有一处 UTC |
| 24 | 飞书通知与失败重试 | §7 | [~] | **`feishu-notification.ts`**：队列表写入、重试间隔与模拟发送；**未**接开放平台 API、**未见**业务路由入队；首页仍写待实现 |
| 25 | 数据保留与轻量主数据 | §8.1–§8.2 | [~] | materials/suppliers；别名词典等 §8.2 细节不足 |
| 26 | 审计/事件日志 | §8.3 | [~] | FA 匹配、建 SC、超收等多类 `audit_logs`；通知/身份等最小集仍不齐 |
| 27 | 首页 Agent 指引 | §9 | [ ] | 有仪表盘卡片；**未**达到 §9 七条完整指引正文 |

---

## 附录决策 34–40（评审补录）— 快速对照

| 决策 | 主题 | 状态 |
|------|------|------|
| 34 | FA 路径 PO 失败重试 3×1min + 显式重试 + §8.3 | [ ] |
| 35 | 单日流水号 01–99，第 100 次失败 | [x]（`number-generator.ts` 达上限抛错） |
| 36 | 官方日历 Asia/Shanghai | [~] 取号用上海日；**FA 有效期判断**在 `approve/route.ts` 仍用 UTC 日字符串 |
| 37 | 飞书通知失败队列与重试 | [~] 模块与重试策略**代码层**具备；真实投递与定时消费未闭环 |
| 38 | 三入口、绑定、隔离 | [ ]（`feishu_bindings` 表有；无完整进线与隔离策略落地） |
| 39 | 自助绑定、无工号邮箱 | [ ] |
| 40 | 无解绑换绑 | [~]（用户侧未做；与 v1.6 决策 40 可视为未触发） |

---

## 建议排期顺序（供排期用）

1. **§4.1 / §5.6 / 决策 34**：批后 FA 命中 → **合并/创建 PO**、失败重试；**统一 FA 有效期与上海日历**（与 `number-generator` 同源）。  
2. **§5.1 / §5.1.1**：material_id 主匹配 + 最低价 + **用户确认**（取消静默 `confirmed`）。  
3. **§6 补强**：RPC/事务或 DB 侧序列表，避免 count 回退的并发撞号。  
4. **§2.2–§2.5**：全 API 统一 `getUserIdentity` + 列表过滤；替换可伪造头为会话身份。  
5. **§7 / 决策 37–39**：飞书 API、入队调用点、绑定与三入口。  
6. **Drizzle/DB**：`goods_receipts` 等与运行 SQL **对齐**，消除部署风险。

---

## 与旧快照的关系

- **`project_20260322_170854`**（及仓库内旧路径 `_review_extract/projects`）：无统一取号、无自动 SC、无超收审批骨架、无飞书队列模块；本表若以旧包为对象，多数行应比 172238 **更差一档**（多为 `[ ]`）。  
- 当前表默认以 **`172238`** 为基线。

---

*生成说明：本清单由对照 `project_20260322_172238` 快照（`_review_extract/project_172238/projects`）评审整理，随代码变更请更新状态列。*
