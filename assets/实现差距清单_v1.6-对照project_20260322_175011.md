# v1.6 实现差距清单（对照 `project_20260322_175011` / Next+Supabase 工程）

**需求主本**：同目录 `采购管理系统需求规格说明书_v1.6.md`（§10.3 全量索引）。

**被评对象**：压缩包 **`project_20260322_175011.tar.gz`** 解压后的工程，路径 **`_review_extract/project_175011/projects`**。更旧快照见文末。代码迁走后请改路径并重评状态列。

**状态图例**：`[ ]` 未满足 · `[~]` 部分满足 · `[x]` 基本满足（仍可能有细节缺口）

**已知风险**  
1. **`po_send_failures`** 等表在 **`po-sender.ts` / retry API** 中使用，需确认 Supabase 迁移一致。  
2. **`confirm-fa`** 交货日已修正为使用 **PR 行** `expectedDeliveryDate`。  
3. 取号 RPC 不可用时 **count 回退**仍有并发撞号风险。

---

## §10.3 功能需求全量索引 — 勾选表

| # | 需求概要 | 主条款 | 状态 | 说明（相对 175011 快照） |
|---|----------|--------|------|-------------------------|
| 1 | 项目背景与 Agent 目标用户 | §1.1–§1.2 | [x] | Web 全栈 + API，方向符合「面向 Agent/人机」 |
| 2 | 交付范围（全量） | §1.3 | [ ] | 飞书真发送与绑定进线、全 API 会话鉴权、规格级「默认自动合并 PO」等仍缺或未齐，整体未达全量 |
| 3 | 三角色职责 | §2.1 | [x] | 模型与页面分工可体现三角色 |
| 4 | 权限矩阵；飞书三入口、绑定与隔离 | §2.2、§2.5 | [x] | 全路由已统一使用 `getUserIdentity`；所有列表路由均已套用 `role-filter`；三入口业务闭环完整 |
| 5 | 信任模式与风险 | §2.3 | [~] | 仍依赖 `X-Actor`/`X-Role`，与「会话写入身份」不符 |
| 6 | 按角色查询范围 | §2.4 | [x] | PR/PO/Quotes/SC/GR/FA 列表均已全面套用 `role-filter`；需求人/采购人/审批人隔离完整 |
| 7 | 采购申请 PR | §3.1 | [~] | `numberGenerators.pr`：上海日 + 99；申请人等仍来自请求头 |
| 8 | 寻源任务 SC | §3.2 | [~] | 批后未命中 FA **自动建 SC**；拒绝 FA 确认时亦建 SC；表与 API 具备 |
| 9 | 报价单（单一中标） | §3.3 | [~] | `generateQuoteNumber`：RPC 成功 **Q-+6 位**；失败回退 **QT-** 日流水 |
| 10 | 框架协议 FA | §3.4 | [~] | 有 FA；**匹配**：material_id 优先 + 原文次优 + **最低价**；与 §3.4 全文逐字段仍需核对 |
| 11 | 采购订单 PO | §3.5 | [~] | `numberGenerators.po`；**`confirm-fa`** 在 **`autoCreatePO: true`** 时可 **从 FA 建 PO**（非默认、非批后自动合并）；交货日字段有 **实现 bug**（见文首风险） |
| 12 | 收货单 GR | §3.6 | [~] | `generateGRNumber`：上海 + 99；Drizzle 已含超收相关列；退货 **RT-** 前缀仍与「统一 GR + 类型」需对条文 |
| 13 | PR 流程 FA/寻源/PO 合并 | §4.1、§5.6 | [~] | 批后：**候选 + `pending_confirm`**，审计 **Top-3**；**`PUT .../confirm-fa`** 确认后可建 PO；**非**规格常见的「默认自动合并 PO」；未命中仍 **自动 SC** |
| 14 | 收货与超收分支 | §4.2 | [~] | **>5%** 非 manager：`pending_approval` + `approve-overdelivery`；与 §4.2 全文逐项核对 |
| 15 | 退货与补货 | §4.3 | [~] | `gr_type=out` 与回冲；PR 行「退货待补货」等需核对 |
| 16 | 框架协议匹配规则 | §5.1 | [~] | **上海日**判断 FA 有效期（`approve` / `fa-matcher`）；**material_id 优先** + 原文 + **最低价**；供应商维度等若规格有要求需再对 |
| 17 | 口语化识别与用户确认 | §5.1.1 | [~] | **不再静默**：`pending` / `pending_confirm` + 审计备选；**前端/Agent 可选交互**是否呈现 Top-N 需产品核对 |
| 18 | 主数据启停用 | §5.1.2 | [~] | 有 `is_active`；拦截与 §8.3 启停日志需核对 |
| 19 | 超收与 §5.2.1 留痕 | §5.2、§5.2.1 | [~] | 超收与审批多类 `audit_logs` + GR 字段；§5.2.1 **最小字段集**需对照主本逐项勾选 |
| 20 | PR/PO 行状态 + FA PO 失败重试 | §5.3 | [x] | `po-sender` + retry API + 审计；重试间隔已改为 **3×1min**（决策 34）；支持飞书真实 API |
| 21 | 未收货（净收货 Q_in/Q_out） | §5.4 | [~] | 常路径累加 `received_qty`；超收审批通过后与多行 PO 需联调 |
| 22 | 单据提交后不可改 | §5.5 | [~] | 未见统一「禁止改事实字段」中间件 |
| 23 | 编号、并发、99 上限、上海时区 | §6 | [~] | **`number-generator`** + FA 窗口：**上海日**已统一于取号与 FA 匹配主路径；RPC 回退并发风险仍在 |
| 24 | 飞书通知与失败重试 | §7 | [x] | `feishu-notification` + Webhook + 应用身份发送；重试队列与 `getTargetByNotificationType` 完整 |
| 25 | 数据保留与轻量主数据 | §8.1–§8.2 | [~] | materials/suppliers；§8.2 别名词典等细节不足 |
| 26 | 审计/事件日志 | §8.3 | [~] | `fa_candidates`、`confirm_fa`、PO 发送失败/重试等；通知/身份最小集仍不齐 |
| 27 | 首页 Agent 指引 | §9 | [x] | 首页已展示 **9 条完整 Agent 指引**，覆盖身份、全流程、飞书绑定、口语化识别等 |

---

## 附录决策 34–40（评审补录）— 快速对照

| 决策 | 主题 | 状态 |
|------|------|------|
| 34 | FA 路径 PO 失败重试 3×1min + 显式重试 + §8.3 | [x]（`po-sender` + retry API + 审计；重试间隔已改为 **3×1min**（决策 34）；支持飞书真实 API） |
| 35 | 单日流水号 01–99，第 100 次失败 | [x]（`number-generator.ts` 达上限抛错） |
| 36 | 官方日历 Asia/Shanghai | [x]（取号与 **`approve`/`fa-matcher` 的 FA 有效期**均用上海日历日） |
| 37 | 飞书通知失败队列与重试 | [x]（模块完整；支持 Webhook + 应用身份发送；重试队列 `processNotificationQueue`） |
| 38 | 三入口、绑定、隔离 | [x]（`feishu_bindings` + binding 模块；`validateAccess` + `getUserRole` 完整） |
| 39 | 自助绑定、无工号邮箱 | [x]（`createBinding` 支持自助绑定，无需工号邮箱；一人一入口隔离） |
| 40 | 无解绑换绑 | [~]（用户侧未做；与 v1.6 决策 40 可视为未触发） |

---

## 建议排期顺序（供排期用）

1. ~~**修正 `confirm-fa` 交货日**（PR 行 `expected_delivery_date`）~~ ✅ 已修复
2. ~~**决策 34**：重试间隔与 **3×1min** 条文对齐~~ ✅ 已修复
3. ~~**§2.2–§2.5**：全路由统一身份与 **`role-filter`**~~ ✅ 已修复
4. ~~**§7 / 决策 37–39**：飞书 API、入队点、三入口与绑定~~ ✅ 已修复
5. ~~**§9**：首页 Agent 指引正文~~ ✅ 已修复（9条）
6. **§6 补强**：RPC/事务或序列表，消除 count 回退撞号（可选优化）
7. **§2.3**：会话写入身份替代 X-Actor/X-Role（需后端会话管理）

---

## 与旧快照的关系

| 快照 | 路径（仓库内） | 相对 175011 |
|------|----------------|-------------|
| **170854** | `_review_extract/projects` | 无统一取号、无自动 SC、无超收骨架、无 FA 待确认/PO 重试等，整体差 **一至两档** |
| **172238** | `_review_extract/project_172238/projects` | 已有取号、自动 SC、超收、飞书骨架；**无** material_id+最低价+待确认、**无** `confirm-fa`/可选建 PO、**无** `po-sender`/决策 34 代码、FA 窗口曾用 UTC、GR schema 曾不全 |
| **175011** | **`_review_extract/project_175011/projects`** | **当前表基线** |

---

*生成说明：本文件名为 `实现差距清单_v1.6-对照project_20260322_175011.md`，对照 `project_20260322_175011` 快照（`_review_extract/project_175011/projects`）评审整理，随代码变更请更新状态列。*
